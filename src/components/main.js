import React from 'react';
import { Navbar } from 'react-bootstrap';

const Main = React.createClass({
  render: function() {
    return (
      <span>
        <Navbar>Main</Navbar>
      </span>
    );
  }
})

export default Main;
